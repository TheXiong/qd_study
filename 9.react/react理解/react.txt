1.Reactjs的优势

  Reactjs能带来什么好处？目前能看到的就是数据和DOM结构分离了，可以随意更改DOM结构和样式而不会影响数据展示；组件的复用和随意组合使代码变得清晰高效。之后的几章大家还能看到：batch update可以减少对DOM的操作使运行更快速；巧妙的事件代理机制使大数据下事件响应依然快速、内存占有率依然保持较低水平；Reactjs还支持服务端渲染。

2.DOM操作慢

  真实DOM的属性多，创建时耗时长，消耗内存多，并且查询时慢。
  如果用更轻量级的 JS 对象来代替复杂的 DOM 节点，然后把对 DOM 的 diff 操作转移到 JS 对象，就可以避免大量对 DOM 的查询操作。这种方式称为 Virtual DOM 。

3.DOM 是很慢的，除了前面说到的它的复杂和庞大，还有另一个原因就是重排和重绘。
  
  批处理

4.父节点的变更也导致了它们的重新渲染，多做了无用操作

  裁剪 ---- shouldComponentUpdate

5.dom diff

  》类型不同，直接卸载旧dom（包括其子节点），再重新构建新dom进行挂载
  》属性不同，仅更改属性，不重新构建，处理完成后递归更新子节点
  》子节点（可能多个），有key的并且key相同的进行比较（新的没有改key则卸载旧节点，旧的没有就新建节点），没key的顺序比较